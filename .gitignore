# Python
__pycache__/
*.py[cod]

# Secrets / lokale Konfiguration
.env
# Google OAuth Client-Secret-Dateien (NIEMALS committen!)
client_secret*.json
*credentials*.json
token.json
refresh_token.txt
# Twitch Broadcaster Refresh Token (Channel Points) – NIEMALS committen!
twitch_refresh_token.txt
# Persoenliches Hotkey-Skript mit Token (Vorlage: skip.ahk.example)
skip.ahk

# Laufzeit-Daten (gehören aufs Railway-Volume, nicht ins Repo)
*.db
queue.db
streamlist.txt
